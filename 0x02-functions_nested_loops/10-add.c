#include "main.h"
/**
 * add- function that adds two integers and returns the result
 * @a: integer to be added to integer b
 * @b: integer to be added to integer a
 * Return: sum of the 2 intergers
 */
int add(int a, int b)
{
int s = a + b;
return (s);
}
